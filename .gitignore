# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfplan.txt
tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Local tooling
.claude/

# Open WebUI local integration (contains a LiteLLM virtual key + DB password)
localtest_openwebui/.env
