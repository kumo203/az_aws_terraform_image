# Value comes from `terraform output` in aws_bootstrap/ after `terraform
# apply` there. Changes on every bootstrap apply (random suffix), so update
# this file before applying aws_tf/.
name_suffix = "REPLACE_WITH_bootstrap_output_name_suffix"
